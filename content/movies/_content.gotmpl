{{ range $movie := hugo.Data.movies }}
  {{ $params := dict
    "id" $movie.id
    "runtime" $movie.runtime
    "genres" $movie.genres
    "channels" (int (partial "channel-count.html" (dict "id" $movie.id "type" "movie")))
  }}

  {{ $page := dict
    "kind" "page"
    "title" $movie.title
    "dates" (dict "date" (time $movie.date))
    "params" $params
    "slug" $movie.id
    "path" $movie.id
  }}

  {{ $.AddPage $page }}
{{ end }}
